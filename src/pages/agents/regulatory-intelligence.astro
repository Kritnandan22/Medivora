---
import BaseLayout from '~/layouts/BaseLayout.astro';

const domainColor = '#1cc9a3'; // Regulatory Affairs color
---

<BaseLayout
  title="Regulatory Intelligence Agent — Medivora"
  description="Architecture and capabilities of the Medivora Regulatory Intelligence Agent for continuous multi-agency monitoring and actionable intelligence synthesis."
>
  <article class="agent-detail-page">
    
    <!-- ── HERO ─────────────────────────────────────────────── -->
    <header class="agent-hero">
      <div class="wrap">
        <div class="breadcrumb">
          <a href="/agents">← Back to Agents</a>
        </div>
        
        <div class="hero-meta">
          <span class="domain-tag" style={`--dc: ${domainColor}`}>Regulatory Affairs</span>
          <span class="phase-tag">Surveillance Track</span>
          <span class="date-tag">Last reviewed: Jun 2025</span>
        </div>
        
        <h1 class="agent-title">Regulatory Intelligence Agent</h1>
        <p class="agent-tagline">Real-time monitoring of global health authority guidelines and mandates.</p>

        <div class="agent-hero-pattern" style="--agent-accent: rgba(6,182,212,0.12)"></div>
        
        <div class="reading-meta">
          <span class="reading-meta-item">📖 13 min read</span>
          <span class="reading-meta-item">📅 Updated 2025-06-02</span>
          <button class="btn-print" onclick="window.print()">🖨 Save as PDF</button>
        </div>

        <nav class="pipeline-indicator" aria-label="ICSR Pipeline Position">
          <a href="/agents/intake-triage" class="pipeline-step">Intake</a>
          <span class="pipeline-arrow">→</span>
          <a href="/agents/meddra-coding" class="pipeline-step">Coding</a>
          <span class="pipeline-arrow">→</span>
          <a href="/agents/medical-review" class="pipeline-step">Review</a>
          <span class="pipeline-arrow">→</span>
          <a href="/agents/aggregate-reporting" class="pipeline-step">Reporting</a>
          <span class="pipeline-arrow">→</span>
          <a href="/agents/literature-monitoring" class="pipeline-step">Literature</a>
          <span class="pipeline-arrow">→</span>
          <a href="/agents/regulatory-intelligence" class="pipeline-step active">RegIntel</a>
        </nav>

        <div class="agent-metrics">
          <div class="metric-box">
            <span class="mb-val">24/7<sup>[1]</sup></span>
            <span class="mb-lbl">Global regulatory surveillance</span>
          </div>
          <div class="metric-box">
            <span class="mb-val">100%<sup>[2]</sup></span>
            <span class="mb-lbl">Citation-grounded responses</span>
          </div>
          <div class="metric-box">
            <span class="mb-val">70+<sup>[3]</sup></span>
            <span class="mb-lbl">Health authorities monitored</span>
          </div>
        </div>
        <div style="margin-top:1.5rem; padding-top:1rem; border-top:1px solid rgba(255,255,255,0.06);">
          <p style="font-family:var(--font-mono); font-size:0.62rem; text-transform:uppercase; letter-spacing:0.1em; color:rgba(241,236,224,0.4); margin-bottom:0.6rem;">References & Sources</p>
          <p style="font-size:0.72rem; color:rgba(241,236,224,0.5); margin:0.2rem 0;">[1] ICH E2A — Requirement for continuous monitoring <span class="" style="font-size:0.6rem;">(Design Principle)</span></p>
          <p style="font-size:0.72rem; color:rgba(241,236,224,0.5); margin:0.2rem 0;">[2] Internal RAG architecture design — no uncited claims <span class="" style="font-size:0.6rem;">(Design Principle)</span></p>
          <p style="font-size:0.72rem; color:rgba(241,236,224,0.5); margin:0.2rem 0;">[3] EMA, FDA, PMDA, MHRA, TGA, HC, Anvisa + 60 local <span class="" style="font-size:0.6rem;">(Coverage)</span></p>
        </div>

        <div class="reviewed-by-band">
          <div class="rb-icon">✓</div>
          <div class="rb-text">
            Content reviewed by <strong>Kritnandan</strong>, B.Tech BSBE (IIT Kanpur) | Last review: Jun 2025 | Review cadence: Quarterly
          </div>
        </div>
      </div>
    </header>

    <!-- ── THE PROBLEM ──────────────────────────────────────── -->
    <section class="section-light problem-section">
      <div class="wrap content-grid">
        <div class="cg-main">
          <h2>The Information Bottleneck</h2>
          <p class="lead-text">
            Regulatory intelligence teams spend countless hours manually tracking updates across fragmented global health authority websites. Missing a critical guidance update, an unprecedented approval pathway, or a sudden safety communication can delay a multi-million dollar launch or result in non-compliance.
          </p>
          <p>
            The Medivora Regulatory Intelligence Agent solves this by continuously ingesting, translating, and semantically structuring unstructured regulatory data. It doesn't just surface updates; it reads the 100-page PDF guidance documents and cross-references them against your active product pipeline to generate targeted impact assessments.
          </p>
        </div>
        <aside class="cg-side info-panel">
          <h4>Agent Profile</h4>
          <dl class="agent-specs">
            <dt>Data Inputs</dt>
            <dd>FDA Dockets, EMA Document Library, PMDA DB, Health Canada HPFB, MHRA Alerts.</dd>
            <dt>Core AI Models</dt>
            <dd>Enterprise LLM (e.g. Gemini / GPT-4 / Claude) for reasoning, Custom Entity Extraction (NER).</dd>
            <dt>Key Outputs</dt>
            <dd>Structured Executive Briefings, Veeva Vault RIM entries, automated email digests.</dd>
            <dt>Standards</dt>
            <dd>ICH Q8–Q12, 21 CFR Parts 210/211/314, EMA CHMP guidelines.</dd>
          </dl>
        </aside>
      </div>
    </section>

    <!-- ── ARCHITECTURE ─────────────────────────────────────── -->
    <section class="section-dark arch-section">
      <div class="wrap">
        <h2>Agent Architecture &amp; Workflow</h2>
        <p class="section-desc">
          A multi-agent orchestration architecture designed for high-stakes regulatory processing. The system separates ingestion, reasoning, and human-in-the-loop (HITL) review to maintain absolute traceability and GxP compliance.
        </p>
        
        <div class="diagram-container">
          <pre class="mermaid">
flowchart LR
    classDef external fill:#1a1a1a,stroke:#333,stroke-width:1px,color:#888
    classDef input fill:#112a22,stroke:#1cc9a3,stroke-width:1px,color:#fff
    classDef agent fill:#0a192f,stroke:#64ffda,stroke-width:1px,color:#fff
    classDef db fill:#2d1b2e,stroke:#e05c8a,stroke-width:1px,color:#fff
    classDef hitl fill:#3a2a00,stroke:#e8a838,stroke-width:1px,color:#fff
    classDef output fill:#112a22,stroke:#1cc9a3,stroke-width:2px,color:#fff

    subgraph "External Regulatory Sources"
        direction TB
        FDA["FDA DailyMed / Dockets"]:::external
        EMA["EMA Document Library"]:::external
        PMDA["PMDA (Japan)"]:::external
    end

    subgraph "Ingestion & Processing Layer"
        direction TB
        Scraper["Web Scrapers & API Connectors"]:::input
        Translation["Translation Agent<br>(Contextual Med-Term Translation)"]:::agent
        VectorDB[("Vector Database<br>(Pinecone/Weaviate)")]:::db
    end

    subgraph "Reasoning Layer (The Brain)"
        direction TB
        Orchestrator(["Orchestrator Agent<br>(Enterprise LLM)"]):::agent
        PipelineDB[("Internal Pipeline /<br>Product Portfolio DB")]:::db
    end

    subgraph "Action & Output Layer"
        direction TB
        HITL(["Human-in-the-Loop<br>(Subject Matter Expert)"]):::hitl
        BriefGen["Briefing Generation Agent"]:::agent
        Veeva["Veeva Vault RIM<br>API Update"]:::output
        Alert["Stakeholder Email Alert"]:::output
    end

    FDA --> Scraper
    EMA --> Scraper
    PMDA --> Scraper

    Scraper --> Translation
    Translation --> VectorDB

    VectorDB -- "Semantic Search (New vs Old)" --> Orchestrator
    PipelineDB -- "Product Context" --> Orchestrator

    Orchestrator -- "Identifies High-Impact Change" --> HITL
    HITL -- "Approves Assessment" --> BriefGen
    HITL -. "Rejects / Modifies" .-> Orchestrator

    BriefGen --> Veeva
    BriefGen --> Alert

    linkStyle default stroke:#444,stroke-width:2px;
          </pre>
        </div>
      </div>
    </section>

    <!-- ── CORE CAPABILITIES ────────────────────────────────── -->
    <section class="section-light caps-section">
      <div class="wrap">
        <h2 class="section-title-dark">Core Capabilities</h2>
        <div class="caps-grid">
          <div class="cap-card">
            <div class="cap-icon">1</div>
            <h3>Real-Time Agency Ingestion</h3>
            <p>Monitors 15+ global health authority websites simultaneously. Ingests raw unstructured text from FDA Dockets, EMA PDF guidelines, and MHRA alerts the moment they are published. Capable of parsing complex tabular data inside PDFs.</p>
          </div>
          <div class="cap-card">
            <div class="cap-icon">2</div>
            <h3>Semantic Pipeline Matching</h3>
            <p>Cross-references incoming regulatory text against your internal pipeline database. Instead of keyword matching, it uses semantic vector search to understand if a new rule impacts a molecule's specific mechanism of action or proposed indication.</p>
          </div>
          <div class="cap-card">
            <div class="cap-icon">3</div>
            <h3>Automated Gap Analysis</h3>
            <p>Generates immediate dossier gap analyses. When the FDA releases a revised draft guidance for a specific therapeutic area, the agent automatically identifies which sections of an upcoming IND or NDA are now non-compliant.</p>
          </div>
          <div class="cap-card">
            <div class="cap-icon">4</div>
            <h3>Competitive Labelling Intelligence</h3>
            <p>Tracks competitor label updates (SmPC/USPI), REMS modifications, and post-market safety requirements to provide early warning of class-wide safety concerns or shifting efficacy standards.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- ── TECHNICAL IMPLEMENTATION ─────────────────────────── -->
    <section class="section-light tech-section">
      <div class="wrap content-grid">
        <div class="cg-main">
          <h2 class="section-title-dark">Technical Implementation Guide</h2>
          <p class="lead-text">
            Building this agent requires a multi-agent orchestration framework connected to a highly precise Retrieval-Augmented Generation (RAG) pipeline.
          </p>
          
          <h3>1. The RAG Pipeline (Vector Search)</h3>
          <p>
            Regulatory intelligence cannot rely on an LLM's base knowledge. The agent uses a Vector Database (like Pinecone or Weaviate) to store historical agency guidance. When a new document arrives, the agent queries the vector database to find the exact precedent or the previous version of the guidance to perform a "diff" analysis.
          </p>

          <h3>2. LLM Selection: The Context Window</h3>
          <p>
            Regulatory PDFs frequently exceed 100 pages. We recommend an <strong>Enterprise LLM (such as Gemini 1.5 Pro, GPT-4o, or Claude 3.5 Sonnet)</strong> as the core reasoning engine. A massive context window allows the model to hold an entire clinical guidance document in memory simultaneously, ensuring no cross-references or footnotes are missed during analysis.
          </p>

          <h3>3. Task-Specific Sub-Agents</h3>
          <p>
            The Orchestrator dispatches work to specialists:
          </p>
          <ul class="tech-list">
            <li><strong>Translation Agent:</strong> Specifically fine-tuned on medical/regulatory terminology (e.g., translating PMDA Japanese text accurately).</li>
            <li><strong>Extraction Agent:</strong> Uses strict JSON schema enforcement to pull dates, ruling types, and affected INNs.</li>
            <li><strong>Formatting Agent:</strong> Converts the raw analysis into a Veeva Vault-compliant XML or Word document format.</li>
          </ul>
        </div>
        <aside class="cg-side visual-panel">
          <div class="mock-ui">
            <div class="mui-header">
              <span class="mui-title">Alert: High Impact Regulatory Change</span>
              <span class="mui-date">Today, 09:41 AM</span>
            </div>
            <div class="mui-body">
              <div class="mui-pill mui-red">Impact Score: 92/100</div>
              <h4>FDA Draft Guidance: Clinical Considerations for Accelerated Approval</h4>
              <p class="mui-desc"><strong>Agent Analysis:</strong> The FDA has updated evidentiary requirements for single-arm trials. This directly impacts our Phase 2b study design for Molecule X (NCT054321).</p>
              <div class="mui-action">Review Full Gap Analysis →</div>
            </div>
          </div>
        </aside>
      </div>
    </section>

    <!-- ── REGULATORY GUARDRAILS ────────────────────────────── -->
    <section class="section-dark guardrails-section">
      <div class="wrap">
        <h2>GxP Compliance & Guardrails</h2>
        <p class="section-desc">AI in a regulatory setting must be determinable, auditable, and human-supervised.</p>
        
        <div class="guard-grid">
          <div class="guard-card">
            <h4>Absolute Traceability</h4>
            <p>Every claim made in the agent's executive summary contains a hyperlinked citation pointing directly to the specific paragraph in the source regulatory PDF. Hallucinations are mitigated through strict instruction-tuning to only cite retrieved text.</p>
          </div>
          <div class="guard-card">
            <h4>Human-in-the-Loop (HITL)</h4>
            <p>The agent operates as a powerful analyst, not a final decision-maker. Any structural change recommended to a dossier or pipeline strategy must be approved by a designated Subject Matter Expert (SME) before integration into the QMS.</p>
          </div>
          <div class="guard-card">
            <h4>Computer Software Assurance (CSA)</h4>
            <p>The agent framework is designed to align with FDA CSA guidance. Critical thinking is applied to testing the RAG pipeline's retrieval accuracy, while automated test scripts continuously validate the API connections to health authorities.</p>
          </div>
        </div>
      </div>
    </section>

  
    <!-- ── UNDER THE HOOD ───────────────────────────────────── -->
    <section class="section-dark under-hood-section">
      <div class="wrap">
        <span class="eyebrow">Under the Hood</span>
        <h2>Architectural Deep Dive</h2>
        
        <div class="uh-grid">
          <div class="uh-col">
            <h3>Health Authority Knowledge Graph</h3>
            <p>The agent connects to a Retrieval-Augmented Generation (RAG) backend that is continually synced with regulatory portals (FDA, EMA, PMDA). This prevents the LLM from relying on outdated training data.</p>
            <div class="code-block-wrapper">
              <div class="code-header"><span class="code-dot r"></span><span class="code-dot y"></span><span class="code-dot g"></span><span class="code-title">tool_call.py</span></div>
              <pre is:raw><code>def query_regulatory_guidance(agency: str, topic: str):
    """
    Tools available to the Regulatory Agent.
    """
    filters = {"agency": agency, "document_type": "guidance"}
    
    # Retrieve relevant document chunks
    context = vector_db.search(
        query=topic, 
        filter=filters,
        top_k=3
    )
    
    return [
      {"source": c.url, "text": c.text, "effective_date": c.date} 
      for c in context
    ]</code></pre>
            </div>
          </div>
          
          <div class="uh-col">
            <h3>Citation-Forced Generation</h3>
            <p>To ensure trust, the system prompt forces the agent to cite the specific paragraph and document version for every regulatory claim it makes.</p>
            <div class="code-block-wrapper">
              <div class="code-header"><span class="code-dot r"></span><span class="code-dot y"></span><span class="code-dot g"></span><span class="code-title">system_prompt.txt</span></div>
              <pre is:raw><code>You are a Regulatory Intelligence Agent.

RULES FOR ANSWERING:
1. Base your answer STRICTLY on the retrieved context documents.
2. For every regulatory requirement stated, append an inline citation [Source ID].
3. If the retrieved context does not contain the answer, reply: "I do not have sufficient information in the current regulatory database to answer this."
4. Never guess a submission deadline.
5. Highlight differences between FDA and EMA requirements if applicable.</code></pre>
            </div>
          </div>
        </div>

        <div class="guardrails-box">
          <h3>Guardrails & Failure Modes</h3>
          <ul class="guardrail-list">
            <li><strong>Hallucinating Deadlines:</strong> The highest risk for this agent is hallucinating a regulatory deadline (e.g., stating 15 days instead of 7 days). The citation-forcing prompt significantly mitigates this.</li>
            <li><strong>Draft vs. Final Guidance:</strong> The RAG database metadata must strictly differentiate between "Draft Guidance" and "Final Rule". The agent is instructed to explicitly state when it is citing draft guidance.</li>
            <li><strong>Local Nuances:</strong> Global regulations are well-documented, but local affiliate regulations (e.g., specific rules in smaller LATAM or APAC markets) often lack digitized, English-language source documents for the agent to retrieve.</li>
          </ul>
        </div>

      </div>
    </section>

  
    <!-- ── SAMPLE DEMO ──────────────────────────────────────── -->
    <section class="section-dark" id="sample-demo">
      <div class="wrap">
        <span class="eyebrow">Interactive Demo</span>
        <h2>See It in Action</h2>
        <h3>Sample Case Walkthrough</h3>
        <span class="synthetic-watermark">Synthetic Data — Illustrative Only</span>
        <div class="sample-demo">
          <div class="demo-panel">
            <div class="demo-panel-header">Input</div>
            <div class="demo-panel-body"><pre is:raw style="white-space:pre-wrap; margin:0; font-size:0.8rem; color:#a8b2d1;">Query: What are the expedited reporting timelines for fatal SUSARs across FDA, EMA, and PMDA?</pre></div>
          </div>
          <div class="demo-panel">
            <div class="demo-panel-header">Agent Output</div>
            <div class="demo-panel-body"><pre is:raw style="white-space:pre-wrap; margin:0; font-size:0.8rem; color:#a8b2d1;">RESPONSE:\n\n1. **FDA** — Fatal/life-threatening SUSARs must be reported within **7 calendar days** of sponsor awareness [Source: 21 CFR 312.32(c)(2)]\n\n2. **EMA** — Fatal/life-threatening SUSARs require reporting within **7 days** with follow-up within 8 additional days [Source: EU CT Regulation 536/2014, Article 42]\n\n3. **PMDA** — Fatal SUSARs require reporting within **7 days** [Source: PMDA Notification No. 0326001]</pre></div>
          </div>
        </div>
      </div>
    </section>

    <!-- ── RISK & COMPLIANCE ────────────────────────────────── -->
    <section class="section-dark" id="risk-compliance" style="border-top: 1px solid rgba(6,182,212,0.1);">
      <div class="wrap">
        <span class="eyebrow">Risk & Compliance</span>
        <h2>Failure Modes, Validation & Integration</h2>
        
        <h3>Risk & Failure Mode Matrix</h3>
        <table class="risk-matrix">
          <thead><tr>
            <th>Failure Mode</th><th>Severity</th><th>Likelihood</th><th>Mitigation</th><th>Residual Risk</th>
          </tr></thead>
          <tbody>
            <tr><td>Hallucinated regulatory deadline</td><td class="risk-high">Critical</td><td class="risk-low">Low</td><td>Citation-forced generation + RAG grounding</td><td class="risk-low">Low</td></tr>
            <tr><td>Draft guidance cited as final rule</td><td class="risk-high">High</td><td class="risk-med">Medium</td><td>Metadata tagging (Draft vs. Final) in RAG DB</td><td class="risk-low">Low</td></tr>
            <tr><td>Missed local market regulation</td><td class="risk-med">Medium</td><td class="risk-high">High</td><td>Expanding RAG coverage + human local affiliate review</td><td class="risk-med">Medium</td></tr>
            <tr><td>Outdated guideline version cited</td><td class="risk-med">Medium</td><td class="risk-med">Medium</td><td>Automated freshness scoring + re-indexing CRON</td><td class="risk-low">Low</td></tr>
          </tbody></table>
        
        <h3>Validation & Qualification</h3>
        <p style="font-size:0.9rem; color:rgba(241,236,224,0.7); margin-bottom:1rem;">This agent is classified under GAMP 5 as a <strong>Category 5</strong> (Custom/Configurable) system requiring full CSV/CSA qualification.</p>
        <div style="display:flex; gap:1rem; flex-wrap:wrap;">
          <span class="gamp-badge">GAMP 5 — Category 5</span>
          <span class="gamp-badge">21 CFR Part 11 Compliant</span>
          <span class="gamp-badge">EU Annex 11 Ready</span>
        </div>
        
        <h3>Integration Points</h3>
        <div class="integration-grid">
          <div class="integration-chip"><div><div>FDA CDER Portal</div><div class="ic-type">Web Scraper</div></div></div>
          <div class="integration-chip"><div><div>EMA Guidelines DB</div><div class="ic-type">API</div></div></div>
          <div class="integration-chip"><div><div>PMDA Portal</div><div class="ic-type">Web Scraper</div></div></div>
          <div class="integration-chip"><div><div>MHRA Portal</div><div class="ic-type">Web Scraper</div></div></div>
          <div class="integration-chip"><div><div>Vector DB (RAG)</div><div class="ic-type">Knowledge Base</div></div></div>
        </div>
        
        <h3>Implementation Timeline</h3>
        <div class="impl-timeline">
          <div class="impl-phase phase-active">
            <div class="impl-phase-num">Phase 1</div>
            <div class="impl-phase-name">Pilot</div>
            <div class="impl-phase-dur">4–6 weeks</div>
            <div class="impl-phase-desc">Deploy on a single product with limited case volume. Validate extraction accuracy and HITL workflows.</div>
          </div>
          <div class="impl-phase">
            <div class="impl-phase-num">Phase 2</div>
            <div class="impl-phase-name">UAT & Validation</div>
            <div class="impl-phase-dur">6–8 weeks</div>
            <div class="impl-phase-desc">Formal IQ/OQ/PQ testing. GAMP 5 documentation. CSV/CSA qualification. 21 CFR Part 11 compliance review.</div>
          </div>
          <div class="impl-phase">
            <div class="impl-phase-num">Phase 3</div>
            <div class="impl-phase-name">Production</div>
            <div class="impl-phase-dur">Ongoing</div>
            <div class="impl-phase-desc">Full deployment across product portfolio. Continuous monitoring, model retraining, and quarterly validation reviews.</div>
          </div>
        </div>
      </div>
    </section>

    <!-- ── VENDOR LANDSCAPE ─────────────────────────────────── -->
    <section class="section-dark" id="vendor-landscape" style="border-top: 1px solid rgba(6,182,212,0.1);">
      <div class="wrap">
        <span class="eyebrow">Market Context</span>
        <h2>Vendor Landscape</h2>
        <h3>Vendor Landscape</h3>
        <p style="font-size:0.85rem; color:rgba(241,236,224,0.6); margin-bottom:1rem;">The following vendors offer capabilities in this agent category. This comparison is vendor-neutral and does not constitute an endorsement.</p>
        <table class="vendor-table">
          <thead><tr><th>Vendor / Platform</th><th>Capability</th><th>Market Segment</th></tr></thead>
          <tbody>
            <tr><td>Cortellis Regulatory Intelligence</td><td>Clarivate regulatory tracking</td><td>Enterprise</td></tr>
            <tr><td>IQVIA RIM</td><td>Regulatory information management</td><td>Enterprise</td></tr>
            <tr><td>Veeva Vault Registrations</td><td>Cloud-native regulatory submissions</td><td>Enterprise</td></tr>
            <tr><td>Regulatory.ai</td><td>AI-first regulatory intelligence</td><td>Startup</td></tr>
          </tbody></table>
      </div>
    </section>

    <!-- ── RESOURCES & NEXT STEPS ───────────────────────────── -->
    <section class="section-dark" id="resources" style="border-top: 1px solid rgba(6,182,212,0.1);">
      <div class="wrap">
        <span class="eyebrow">Resources</span>
        <h2>Related Reading & Next Steps</h2>
        
        <div class="related-studies">
          <h3>Related Case Studies</h3>
          <div class="study-card-grid">
            <a href="/case-studies/expedited-reporting" class="study-link-card">
              <div class="slc-label">Case Study</div>
              <div class="slc-title">Expedited Reporting Automation</div>
              <div class="slc-desc">Ensuring correct expedited reporting timelines across 70+ health authorities.</div>
            </a>
          </div>
        </div>
        
        <div style="display:flex; gap:1rem; flex-wrap:wrap; margin:2rem 0;">
          <a href="/contact" class="study-link-card" style="flex:1; min-width:250px;">
            <div class="slc-label">Implementing this?</div>
            <div class="slc-title">Download Pilot Checklist →</div>
            <div class="slc-desc">Step-by-step guide to deploying a RegIntel agent in your PV environment.</div>
          </a>
          <a href="/contact" class="study-link-card" style="flex:1; min-width:250px;">
            <div class="slc-label">Evaluating vendors?</div>
            <div class="slc-title">Get the RFP Template →</div>
            <div class="slc-desc">Pre-built questions for evaluating AI-powered regintel solutions.</div>
          </a>
        </div>
        
        <div class="feedback-bar">
          <p>Was this deep dive useful?</p>
          <button class="feedback-btn" onclick="this.classList.add('selected')">👍 Yes</button>
          <button class="feedback-btn" onclick="this.classList.add('selected')">👎 Needs work</button>
          <a href="/contact" style="font-size:0.75rem; color:rgba(241,236,224,0.5); margin-left:auto;">Found an error? Let us know →</a>
        </div>
        
        <div class="version-history">
          <h4>Version History</h4>
          <div class="version-entry">
            <span class="version-tag">v2.0</span>
            <span class="version-date">2025-06-02</span>
            <span>Added Under the Hood section, risk matrix, vendor landscape, sample demos, and citation-sourced KPIs.</span>
          </div>
          <div class="version-entry">
            <span class="version-tag">v1.0</span>
            <span class="version-date">2024-11-01</span>
            <span>Initial architecture and capabilities published.</span>
          </div>
        </div>
        
        <nav class="pipeline-nav">
          <a href="/agents/literature-monitoring">
            <div>
              <span class="pn-label">← Previous Agent</span>
              <span class="pn-name">Literature Monitoring Agent</span>
            </div>
          </a>
          <div></div>
        </nav>
      </div>
    </section>

  </article>

<script is:inline>
  // Copy-to-clipboard for code blocks
  document.querySelectorAll('.code-block-wrapper').forEach(wrapper => {
    wrapper.style.position = 'relative';
    const btn = document.createElement('button');
    btn.className = 'copy-btn';
    btn.textContent = 'Copy';
    btn.addEventListener('click', () => {
      const code = wrapper.querySelector('code');
      if (code) {
        navigator.clipboard.writeText(code.textContent).then(() => {
          btn.textContent = 'Copied!';
          btn.classList.add('copied');
          setTimeout(() => { btn.textContent = 'Copy'; btn.classList.remove('copied'); }, 2000);
        });
      }
    });
    wrapper.appendChild(btn);
  });
  // Feedback buttons
  document.querySelectorAll('.feedback-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      document.querySelectorAll('.feedback-btn').forEach(b => b.classList.remove('selected'));
      btn.classList.add('selected');
    });
  });
</script>
</BaseLayout>

<script is:inline type="module">
  import mermaid from 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.esm.min.mjs';
  mermaid.initialize({
    startOnLoad: true,
    theme: 'base',
    themeVariables: {
      primaryColor: '#1cc9a3',
      primaryTextColor: '#fff',
      primaryBorderColor: '#333',
      lineColor: '#666',
      secondaryColor: '#0a192f',
      tertiaryColor: '#111'
    },
    fontFamily: 'Inter, sans-serif'
  });
</script>

<style>
  /* ── Layout & Typography ───────────────────────── */
  .agent-detail-page {
    background: var(--color-bg);
    min-height: 100vh;
  }
  .wrap { max-width: 1200px; margin: 0 auto; padding: 0 5%; }
  
  .section-light { padding: 5rem 0; background: var(--color-bg); border-bottom: 1px solid var(--color-line); }
  .section-dark { padding: 6rem 0; background: #060e0b; color: #f1ece0; border-bottom: 1px solid rgba(28,201,163,.15); }
  
  .content-grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 4rem;
  }
  @media (max-width: 900px) {
    .content-grid { grid-template-columns: 1fr; gap: 3rem; }
  }

  /* ── Hero ──────────────────────────────────────── */
  .agent-hero {
    background: #060e0b;
    padding: 3rem 0 5rem;
    border-bottom: 1px solid rgba(28,201,163,.15);
    color: #f1ece0;
  }
  .breadcrumb { margin-bottom: 3rem; }
  .breadcrumb a {
    font-size: 0.85rem;
    color: rgba(241,236,224,.5);
    text-decoration: none;
    transition: color 200ms ease;
  }
  .breadcrumb a:hover { color: #1cc9a3; }

  .hero-meta { display: flex; gap: 0.8rem; margin-bottom: 1.5rem; align-items: center; }
  .domain-tag {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--dc);
    background: color-mix(in srgb, var(--dc) 15%, transparent);
    padding: 4px 10px;
    border-radius: 100px;
  }
  .phase-tag {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    letter-spacing: 0.05em;
    color: rgba(241,236,224,.6);
    border: 1px solid rgba(241,236,224,.2);
    padding: 4px 10px;
    border-radius: 100px;
  }

  .agent-title {
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 4vw, 3.5rem);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }
  .agent-tagline { font-size: 1.2rem; color: rgba(241,236,224,.7); max-width: 45em; line-height: 1.5; margin-bottom: 2rem; }

  /* ── Cinematic Images ── */
  .cinematic-image-wrapper {
    margin: 0 auto 3rem;
    max-width: 100%;
    border-radius: var(--radius-lg);
    overflow: hidden;
    border: 1px solid rgba(241, 236, 224, 0.1);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
  }
  .cinematic-img {
    width: 100%;
    height: auto;
    aspect-ratio: 21 / 9;
    object-fit: cover;
    object-position: center 50%;
    display: block;
    transition: transform 0.8s ease;
  }
  .cinematic-image-wrapper:hover .cinematic-img {
    transform: scale(1.02);
  }

  .agent-metrics { display: flex; gap: 1.5rem; flex-wrap: wrap; }
  .metric-box {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    padding: 1.5rem;
    border-radius: 8px;
    min-width: 220px;
  }
  .mb-val {
    display: block;
    font-family: var(--font-mono);
    font-size: 2rem;
    font-weight: 700;
    color: #1cc9a3;
    line-height: 1;
    margin-bottom: 0.5rem;
  }
  .mb-lbl {
    display: block;
    font-size: 0.85rem;
    color: rgba(241,236,224,.6);
    line-height: 1.4;
  }

  /* ── Problem Section ───────────────────────────── */
  .problem-section h2 {
    font-family: var(--font-serif);
    font-size: 2rem;
    margin-bottom: 1.5rem;
    color: var(--color-text);
  }
  .lead-text {
    font-size: 1.15rem;
    line-height: 1.7;
    color: var(--color-text);
    font-weight: 500;
    margin-bottom: 1.5rem;
  }
  .cg-main p {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--color-text-2);
    margin-bottom: 1.5rem;
  }

  .info-panel {
    background: var(--color-surface);
    border: 1px solid var(--color-line);
    padding: 2rem;
    border-radius: 8px;
    height: fit-content;
  }
  .info-panel h4 {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-text);
    margin-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-line);
    padding-bottom: 0.5rem;
  }
  .agent-specs dt {
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: #1cc9a3;
    margin-top: 1.2rem;
    margin-bottom: 0.3rem;
  }
  .agent-specs dt:first-child { margin-top: 0; }
  .agent-specs dd {
    margin: 0;
    font-size: 0.9rem;
    color: var(--color-text-2);
    line-height: 1.5;
  }

  /* ── Architecture Section ──────────────────────── */
  .arch-section h2 {
    font-family: var(--font-serif);
    font-size: 2.2rem;
    margin-bottom: 1rem;
  }
  .section-desc {
    font-size: 1.1rem;
    color: rgba(241,236,224,.7);
    max-width: 50em;
    line-height: 1.6;
    margin-bottom: 3rem;
  }
  .diagram-container {
    background: #0d1a15;
    border: 1px solid rgba(28,201,163,.2);
    border-radius: 12px;
    padding: 2rem;
    overflow-x: auto;
  }

  /* ── Dynamic Graph Animations ──────────────────── */
  :global(.mermaid .edgePath path) {
    stroke-dasharray: 8 4;
    animation: flowLines 1.5s linear infinite;
  }
  @keyframes flowLines {
    to { stroke-dashoffset: -12; }
  }
  
  :global(.mermaid .node) {
    transition: transform 0.3s ease, filter 0.3s ease;
    cursor: pointer;
  }
  :global(.mermaid .node:hover) {
    transform: scale(1.03);
    filter: drop-shadow(0 0 12px rgba(28,201,163,0.4));
  }
  
  /* Pulse effect for Orchestrator */
  :global(.mermaid #Orchestrator) {
    animation: pulseOrch 3s infinite alternate ease-in-out;
  }
  @keyframes pulseOrch {
    0% { filter: drop-shadow(0 0 5px rgba(100,255,218,0.2)); }
    100% { filter: drop-shadow(0 0 20px rgba(100,255,218,0.6)); }
  }

  /* ── Capabilities ──────────────────────────────── */
  .caps-section { background: var(--color-surface); }
  .section-title-dark {
    font-family: var(--font-serif);
    font-size: 2.2rem;
    margin-bottom: 2rem;
    color: var(--color-text);
  }
  .caps-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }
  @media (max-width: 768px) { .caps-grid { grid-template-columns: 1fr; } }
  .cap-card {
    background: var(--color-bg);
    border: 1px solid var(--color-line);
    padding: 2rem;
    border-radius: 8px;
    transition: transform 200ms ease;
  }
  .cap-card:hover { transform: translateY(-3px); border-color: #1cc9a3; }
  .cap-icon {
    width: 40px; height: 40px;
    background: rgba(28,201,163,.1);
    color: #1cc9a3;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-family: var(--font-mono); font-weight: bold; font-size: 1.2rem;
    margin-bottom: 1.2rem;
  }
  .cap-card h3 { font-size: 1.2rem; margin-bottom: 0.8rem; color: var(--color-text); }
  .cap-card p { font-size: 0.95rem; color: var(--color-text-2); line-height: 1.6; }

  /* ── Tech Section ──────────────────────────────── */
  .tech-section h3 { margin-top: 2rem; margin-bottom: 0.8rem; color: var(--color-text); font-size: 1.3rem; }
  .tech-list { padding-left: 1.5rem; margin-top: 1rem; color: var(--color-text-2); line-height: 1.7; }
  .tech-list li { margin-bottom: 0.8rem; }
  .tech-list strong { color: var(--color-text); }

  .mock-ui {
    background: #fff;
    border: 1px solid #e2e8f0;
    border-radius: 8px;
    box-shadow: 0 10px 25px rgba(0,0,0,0.05);
    overflow: hidden;
  }
  :global([data-theme='dark']) .mock-ui {
    background: #111827; border-color: #374151;
  }
  .mui-header { background: #f8fafc; padding: 1rem; border-bottom: 1px solid #e2e8f0; display: flex; justify-content: space-between; align-items: center; }
  :global([data-theme='dark']) .mui-header { background: #1f2937; border-bottom-color: #374151; }
  .mui-title { font-family: var(--font-mono); font-size: 0.75rem; font-weight: bold; color: #dc2626; text-transform: uppercase; }
  .mui-date { font-size: 0.75rem; color: #64748b; }
  .mui-body { padding: 1.5rem; }
  .mui-pill { display: inline-block; padding: 4px 10px; border-radius: 100px; font-size: 0.75rem; font-weight: bold; margin-bottom: 1rem; }
  .mui-red { background: #fee2e2; color: #b91c1c; }
  :global([data-theme='dark']) .mui-red { background: rgba(220,38,38,0.2); color: #ef4444; }
  .mui-body h4 { font-size: 1.1rem; margin-bottom: 0.5rem; color: #0f172a; }
  :global([data-theme='dark']) .mui-body h4 { color: #f1f5f9; }
  .mui-desc { font-size: 0.9rem; color: #475569; line-height: 1.5; margin-bottom: 1.5rem; }
  :global([data-theme='dark']) .mui-desc { color: #94a3b8; }
  .mui-action { color: #2563eb; font-weight: 600; font-size: 0.9rem; cursor: pointer; }
  :global([data-theme='dark']) .mui-action { color: #3b82f6; }

  /* ── Guardrails ────────────────────────────────── */
  .guard-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; }
  @media (max-width: 900px) { .guard-grid { grid-template-columns: 1fr; } }
  .guard-card { background: rgba(255,255,255,0.02); padding: 2rem; border-radius: 8px; border: 1px solid rgba(255,255,255,0.05); }
  .guard-card h4 { font-size: 1.2rem; color: #1cc9a3; margin-bottom: 1rem; }
  .guard-card p { font-size: 0.95rem; color: rgba(241,236,224,.7); line-height: 1.6; }
</style>
