---
import ContentLayout from '~/layouts/ContentLayout.astro';
---

<ContentLayout
  title="Pharmacy Operations & Medication Management"
  description="How AI pharmacy agents free pharmacists from administrative load — from prescription processing and insurance verification to outbound adherence campaigns."
  eyebrow="07 — Pharmacy Operations"
  heading="The counter, <em>unburdened.</em>"
  lede='The "management medicine" frontier — where automation meets the patient. The closest, most validated win is medication adherence; the biggest time-saver is freeing pharmacists from administrative load.'
>
  <section class="stage-section">
    <div class="wrap">
      <div class="stage-grid">
        <article class="stage-block wf-block">
          <div class="block-head">
            <span class="chip wf-chip">Workflow</span>
            <span class="block-label">Dispensing &amp; logistics</span>
          </div>
          <ul>
            <li>Dispensing robotics and automated inventory management</li>
            <li>Prescription processing — refills, transfers and new requests</li>
            <li>Automated refill reminders and routine patient communications</li>
          </ul>
        </article>
        <article class="stage-block ag-block">
          <div class="block-head">
            <span class="chip ag-chip">Agentic</span>
            <span class="block-label">The AI pharmacy assistant</span>
          </div>
          <ul>
            <li>Agents process prescriptions and update the pharmacy system in real time</li>
            <li>Insurance verification, copay/coverage queries and inbound call handling</li>
            <li>Outbound adherence and vaccination campaigns; medication reconciliation that flags discrepancies</li>
            <li>OCR that reads handwritten prescriptions; adherence sensed via smart bottles, dispensers and wearables</li>
          </ul>
        </article>
      </div>
      <blockquote class="stage-proof">
        <strong>The principle:</strong> AI won't replace the pharmacist — it automates low-value
        tasks (one agent reported a ~67% cut in admin time) so clinical judgement and patient care
        get the human hours.
      </blockquote>
      <nav class="stage-nav">
        <a href="/value-chain/supply-chain" class="btn btn-ghost">← Supply Chain</a>
        <a href="/value-chain" class="btn btn-primary">All seven stages →</a>
      </nav>
    </div>
  </section>
</ContentLayout>

<style>
  .stage-section { padding: 3rem 0 5rem; }
  .stage-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.8rem; margin-bottom: 2rem; }
  .stage-block { background-color: var(--color-surface); border: 1px solid var(--color-line-soft); border-radius: var(--radius-md); padding: 1.8rem 1.6rem; }
  .block-head { display: flex; align-items: center; gap: 0.65rem; margin-bottom: 1.1rem; }
  .chip { font-family: var(--font-mono); font-size: 0.64rem; letter-spacing: 0.12em; text-transform: uppercase; padding: 4px 9px; border-radius: var(--radius-xs); }
  .wf-chip { background-color: var(--color-accent); color: white; }
  .ag-chip { background-color: var(--color-signal); color: white; }
  .block-label { font-weight: 600; font-size: 0.96rem; color: var(--color-text); }
  .stage-block ul { list-style: none; display: flex; flex-direction: column; gap: 0.7rem; }
  .stage-block li { font-size: 0.93rem; color: var(--color-text-2); line-height: 1.5; padding-left: 1.1rem; position: relative; }
  .stage-block li::before { content: ''; position: absolute; left: 0; top: 0.55rem; width: 6px; height: 6px; border-radius: 50%; }
  .wf-block li::before { background-color: var(--color-accent); }
  .ag-block li::before { background-color: var(--color-signal); }
  .stage-proof { font-size: 0.95rem; color: var(--color-text-soft); font-style: italic; border-left: 2px solid var(--color-signal); padding: 0.4rem 0 0.4rem 1.1rem; line-height: 1.6; margin-bottom: 2.5rem; }
  .stage-proof strong { font-style: normal; color: var(--color-text); font-weight: 600; }
  .stage-nav { display: flex; gap: 1rem; flex-wrap: wrap; }
  @media (max-width: 680px) { .stage-grid { grid-template-columns: 1fr; } }
</style>
